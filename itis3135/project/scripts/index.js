$(document).ready(function() {
    $("#slider").bxSlider({
        auto: true,
        slideWidth: 300,
        slideMargin: 10,
        pause: 3000,
        adaptiveHeight: true,
       // pagerType:'short',
      // pagerSelector:'#id_pager',

    });
});